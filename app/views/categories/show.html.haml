.row
  .col-sm-8.col-offset-1
    %h1.inline_block
      = @category.name
    = link_to edit_category_path(@category) do
      %i.glyphicon.glyphicon-pencil
    %ul
      - @category.subcategories.each do |subcategory|
        %li=link_to subcategory, subcategory
